% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{logprobs}
\alias{logprobs}
\title{Extract token log probabilities from a Message or list of Messages}
\usage{
logprobs(x, top = FALSE)
}
\arguments{
\item{x}{\code{Message} object, list of \code{Message} objects, or list of lists of \code{Message} objects.}

\item{top}{Logical: If \code{TRUE}, add an \code{alternatives} list column holding the top-k alternative
tokens considered at each position, each as a data.table of \code{token}, \code{logprob} and \code{prob}.
The alternatives are only present when the call also set \code{top_logprobs}.}
}
\value{
For a single \code{Message}, a data.table of \code{position}, \code{token}, \code{logprob} and \code{prob}, or
\code{NULL} when the message carries no log probabilities. For a list, a list of those, one per
element, so the length of the result matches the length of \code{x}.
}
\description{
Returns the per-token log probabilities of the assistant's response, when the call was made
with \code{logprobs = TRUE}. Reading a probability off the first token is better calibrated than
asking a model to emit a number, so this is the basis for scoring a binary question.
}
\details{
Supported on Ollama and OpenAI-compatible backends; Anthropic does not return log
probabilities, so its messages yield \code{NULL} rather than an error.
}
\examples{
# Requires running Ollama server and gemma4:e4b model
\dontrun{
  llm <- create_Ollama("gemma4:e4b", system_prompt = "Answer Yes or No only.")
  msg <- generate(
    llm, "Is the sky blue?",
    think = FALSE, logprobs = TRUE, top_logprobs = 5L
  )
  logprobs(msg)
}
}
\author{
EDG
}
