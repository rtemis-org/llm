% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{responses}
\alias{responses}
\title{Extract response(s) from a Message or list of Messages}
\usage{
responses(x)
}
\arguments{
\item{x}{\code{Message} object, list of \code{Message} objects, or list of lists of \code{Message} objects.
\code{NULL} elements are allowed: \link{llmapply} and \link{agentapply} leave one in the slot of every call
that failed under \code{on_error = "na"}.}
}
\value{
Character vector of assistant responses. Returns \code{NA_character_} in slots where no
assistant message is present (including failed calls), so that the length of the result
matches the length of \code{x}. An agent history returned by \link{generate} yields
only the final assistant response. Attached validation reports are preserved.
}
\description{
Returns the assistant content from a single \code{Message}, from a flat list of \code{Message} objects
(e.g. the output of \link{llmapply} with \code{extract_responses = FALSE}), or from a list of lists of
\code{Message} objects (e.g. the output of \code{map()} on an \code{Agent} with \code{extract_responses = FALSE}).
}
\examples{
# Requires running Ollama server and gemma4:e4b model
\dontrun{
  llmapply(
    c("burgundy", "crimson", "maroon", "ruby", "scarlet"),
    "gemma4:e4b",
    system_prompt = "Return the hexadecimal code for the color provided in format #FFFFFF",
    temperature = 0.2
  ) |> responses()
}
}
\author{
EDG
}
