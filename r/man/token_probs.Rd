% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{token_probs}
\alias{token_probs}
\title{Probability of specific tokens at one position}
\usage{
token_probs(x, tokens, position = 1L)
}
\arguments{
\item{x}{\code{Message} object, list of \code{Message} objects, or list of lists of \code{Message} objects.}

\item{tokens}{Character: Candidate tokens to report probabilities for.}

\item{position}{Integer [1, Inf): Token position to read. Position 1 is the first generated
token, which is the answer only when reasoning is off - a thinking model spends its opening
tokens on the reasoning channel.}
}
\value{
For a single \code{Message}, a named numeric vector, one element per token in \code{tokens}. For
a list, a numeric matrix with one row per element of \code{x} and one column per token.
}
\description{
Reads the probability of each candidate answer directly off the model's token distribution,
rather than parsing a value the model emitted. Asking a binary question and taking \code{P("Yes")}
at the first position is far better calibrated than asking the model for a number.
}
\details{
Candidates are matched on trimmed, case-folded token text, and the probabilities of every
matching alternative are summed, so \code{"Yes"} picks up \code{" Yes"} and \code{"yes"} too. A candidate that
does not appear among the returned alternatives is \code{NA}, not zero: its probability is unknown,
only bounded above by the smallest one returned. Raise \code{top_logprobs} if candidates you care
about come back \code{NA}.
}
\examples{
# Requires running Ollama server and gemma4:e4b model
\dontrun{
  llm <- create_Ollama("gemma4:e4b", system_prompt = "Answer Yes or No only.")
  msg <- generate(
    llm, "Is the sky blue?",
    think = FALSE, logprobs = TRUE, top_logprobs = 5L
  )
  token_probs(msg, c("Yes", "No"))
}
}
\author{
EDG
}
