% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_apple.R
\name{apple_health}
\alias{apple_health}
\title{Read the rtemis-afm Bridge's Health}
\usage{
apple_health(base_url = APPLE_URL_DEFAULT)
}
\arguments{
\item{base_url}{Character: Base URL of the bridge's OpenAI-compatible wire. The health
endpoint is read from the same server, beside \verb{/v1}.}
}
\value{
Named list: The parsed health response.
}
\description{
Reads \code{GET /health} of a running \code{rtemis-afm} bridge and returns its body:
\code{status}, \code{version}, and \code{model}, which carries the served model's \code{id},
\code{name}, \code{availability} (\code{"available"} or \code{"unavailable"}), \code{reason} when
unavailable (\code{"deviceNotEligible"}, \code{"appleIntelligenceNotEnabled"}, or
\code{"modelNotReady"}), and \code{context_window} in tokens.
}
\examples{
# Requires a running rtemis-afm bridge
\dontrun{
  apple_health()
}
}
\author{
EDG
}
