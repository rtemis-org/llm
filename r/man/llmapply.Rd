% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llmapply.R
\name{llmapply}
\alias{llmapply}
\title{Batch submission of prompts to an LLM agent}
\usage{
llmapply(
  X,
  system_prompt,
  model_name = NULL,
  backend = c("ollama", "claude", "openai"),
  use_memory = FALSE,
  tools = NULL,
  max_tool_rounds = 3L,
  output_schema = NULL,
  name = NULL,
  verbosity = 1L,
  extract_responses = TRUE,
  agent = NULL,
  ...
)
}
\arguments{
\item{X}{A vector (atomic or list) to iterate over. Other objects will be coerced by
\link[base:list]{base::as.list}. Each element will form the user prompt for a separate call to the LLM agent.}

\item{system_prompt}{Character: The system prompt to use for all calls.}

\item{model_name}{Character: The name of the model to use. Ignored if \code{agent} is provided.}

\item{backend}{Character: The LLM backend to use. One of "ollama", "claude", or "openai".
Ignored if \code{agent} is provided.}

\item{use_memory}{Logical: If \code{TRUE}, the agent will retain memory across calls. Ignored if
\code{agent} is provided.}

\item{tools}{List of \code{Tool} objects: Tools to use for all calls. Ignored if \code{agent} is provided.}

\item{max_tool_rounds}{Integer: Maximum number of tool use rounds per call. Ignored if \code{agent}
is provided.}

\item{output_schema}{\code{Schema} object, named list, or JSON string defining the expected structure
of the LLM response. Ignored if \code{agent} is provided.}

\item{name}{Character: Name for the agent. Ignored if \code{agent} is provided.}

\item{verbosity}{Integer: Verbosity level for messages. Ignored if \code{agent} is provided.}

\item{extract_responses}{Logical: If \code{TRUE}, extract the content of the assistant message from
the full response. If \code{FALSE}, return the full response object from the agent.}

\item{agent}{Optional \code{Agent} object: Agent to use for generating responses.}

\item{...}{Additional arguments passed to the \code{generate} method for the agent.}
}
\value{
A list of \code{Message} objects or a list of structured responses returned by the agent
}
\description{
Batch submission of prompts to an LLM agent
}
\details{
If \code{agent} is provided, all agent-related arguments (model_name, backend, use_memory, tools,
max_tool_rounds, output_schema, name, verbosity) are ignored. If \code{agent} is not provided, a new
Agent will be created using the provided arguments.
}
\examples{
# Requires running Ollama server with the "gemma4:e4b" model
\dontrun{
  hex <- llmapply(
     c("teal", "orange", "burgundy"),
     system_prompt = "Return the hexadecimal code for the following color in format #FFFFFF",
     model_name = "gemma4:e4b"
   )
}
}
\author{
EDG
}
