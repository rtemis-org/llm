% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_init.R
\name{generate}
\alias{generate}
\title{Generate Method}
\usage{
generate(
  x,
  prompt,
  temperature = NULL,
  top_p = NULL,
  max_tokens = NULL,
  stop = NULL,
  think = NULL,
  output_schema = NULL,
  verbosity = 1L,
  validate_output = TRUE,
  on_validation_failure = c("warn", "collect", "abort"),
  ...
)
}
\arguments{
\item{x}{An object of class LLM or Agent.}

\item{prompt}{Character: The prompt to pass to the model or agent.}

\item{temperature}{Optional numeric [0, 2]: Per-call sampling temperature.}

\item{top_p}{Optional numeric [0, 1]: Nucleus sampling cutoff.}

\item{max_tokens}{Optional integer [1, Inf): Maximum tokens to generate. For Anthropic,
this overrides the config-level value (which is required); for Ollama this maps to
\code{options.num_predict}; for OpenAI-compatible backends this maps to \code{max_tokens}.}

\item{stop}{Optional character: Stop sequence(s). Mapped to \code{stop_sequences} on Anthropic
and \code{options.stop} on Ollama.}

\item{think}{Optional logical or character: Whether to enable model thinking
(reasoning trace) for this call. Character values target \code{gpt-oss}-style local models.}

\item{output_schema}{Optional Schema: Output schema to enforce on this call's response.
If omitted, the object's default schema (if any) is used.}

\item{verbosity}{Integer: Verbosity level.}

\item{validate_output}{Logical: Validate final output locally when a schema is supplied.
Disabling this does not disable the schema sent to the model.}

\item{on_validation_failure}{Character \{"warn", "collect", "abort"\}: Preserve invalid output
and report a styled rtemis.core message (not an R warning), collect silently, or
abort with an error carrying \code{output} and \code{validation}. See \link{validate_output}.}

\item{...}{Additional backend-specific per-call arguments. See Details.}
}
\value{
\code{Message} object or list of \code{Message} objects (for \code{Agent}). With a
schema, \link{validation_results} retrieves the attached validation report. Invalid
output is retained unless explicitly configured to abort. Agent validation
checks only the final answer, before committing that answer to memory.
}
\description{
Generic method for generating text or structured output from LLMs and Agents.
}
\details{
The system prompt is set once at agent (or LLM) construction time and is \strong{not}
overridable per call. Construct a new agent if you need a different system prompt.

Backend-specific extra arguments accepted via \code{...}:
\itemize{
\item \strong{Ollama}: \code{top_k} (integer), \code{seed} (integer), \code{num_ctx} (integer, mapped to
\code{options.num_ctx}), \code{keep_alive} (character duration such as \code{"10m"}, or seconds as a
number, controlling how long the model stays loaded after the request), \code{logprobs}
(logical), \code{top_logprobs} (integer)
\item \strong{OpenAI}: \code{seed} (integer), \code{logprobs} (logical), \code{top_logprobs} (integer [0, 20])
\item \strong{Anthropic}: \code{top_k} (integer)
}

Any argument set to \code{NULL} (the default) falls back to the value baked into the
underlying \code{LLMConfig} at construction time.
}
\examples{
# Requires running Ollama server and gemma4:e4b model
\dontrun{
  agent <- create_agent(
    config_Ollama(
      model_name = "gemma4:e4b",
      temperature = 0.2
    )
  )
  generate(agent, "What is your name?", temperature = 0.7)
}
}
\author{
EDG
}
