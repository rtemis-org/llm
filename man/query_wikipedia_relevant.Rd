% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_wikipedia.R
\name{query_wikipedia_relevant}
\alias{query_wikipedia_relevant}
\title{High-level relevant Wikipedia query pipeline}
\usage{
query_wikipedia_relevant(
  query,
  titles_limit = 10L,
  language = "en",
  verbosity = 1L,
  page_filter = NULL,
  page_filter_mode = c("any", "all", "regex", "function"),
  section_filter = NULL,
  section_filter_mode = c("any", "all", "regex", "function"),
  mode = c("sections", "full_page"),
  max_sections_per_page = NULL,
  clean_refs = TRUE,
  output = c("JSON", "data.table"),
  json_pretty = FALSE
)
}
\arguments{
\item{query}{Character: Search query}

\item{titles_limit}{Integer: number of titles to consider (default 10)}

\item{language}{Character: Wikipedia language code (default "en")}

\item{verbosity}{Integer: Verbosity level (default 1)}

\item{page_filter}{Function|Character|NULL: If function, called as f(title, intro, pageid) -> TRUE/FALSE.
If character vector, treated as keywords. If single string with page_filter_mode == "regex", interpreted as regex.}

\item{page_filter_mode}{Character: one of c("any", "all", "regex", "function"). Default depends on page_filter type.}

\item{section_filter}{Function|Character|NULL: If function, called as f(section_title, section_id, pageid, title, intro) -> TRUE/FALSE.
If character vector, treated as keywords on section titles. If single string with section_filter_mode == "regex", interpreted as regex.}

\item{section_filter_mode}{Character: one of c("any", "all", "regex", "function").}

\item{mode}{Character: one of c("sections", "full_page") determining step 4 behavior (default "sections").}

\item{max_sections_per_page}{Integer|NULL: Optional cap per page (applied after filtering) when mode == "sections".}

\item{clean_refs}{Logical: Clean numeric citation markers (default TRUE).}

\item{output}{Character: One of c("JSON", "data.table") (default "JSON").}

\item{json_pretty}{Logical: Pretty-print JSON (default FALSE).}
}
\value{
data.table or JSON string of rows with columns: page_id, title, section, section_id, text, url
}
\description{
Implements the 4-step flow:
\enumerate{
\item search titles; 2) fetch introductions; 3) filter relevant pages; 4) fetch sections or full page
}
}
\examples{
# Example 1: Sections mode with keyword filter on section titles
# wr <- query_wikipedia_relevant(
#   query = "MAPT",
#   titles_limit = 2,
#   language = "en",
#   section_filter = c("function", "structure"),
#   section_filter_mode = "any",
#   mode = "sections",
#   max_sections_per_page = 3L,
#   output = "data.table",
#   verbosity = 0L
# )

# Example 2: Full page mode with a page-level regex filter
# wr <- query_wikipedia_relevant(
#   query = "MAPT",
#   titles_limit = 2,
#   language = "en",
#   page_filter = "tau|microtubule",
#   page_filter_mode = "regex",
#   mode = "full_page",
#   output = "data.table",
#   verbosity = 0L
# )
}
